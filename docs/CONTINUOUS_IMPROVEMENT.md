# Continuous improvement log

Running daily log of production health checks and improvement suggestions
for amlkit, generated by a scheduled Claude Code routine. Pure append —
each entry is dated; prior entries are never edited or reorganized.

### 2026-08-20

- Health check: **UNKNOWN (not DOWN)** — this environment's egress proxy returned `403 Forbidden` on the CONNECT to `amlkit-720622408077.me-central1.run.app:443` for both `/` and `/about`, and identically for a `www.google.com` control request, confirming it's an organizational network policy blocking outbound HTTPS from this session, not a signal about the site itself. No real health data was obtainable today. If this recurs daily, the check needs an allowlisted egress path or a different verification method (e.g. Cloud Run's own uptime metrics) — flagging in case this needs fixing at the environment level rather than being silently "unknown" every day.
- `scripts/backup_to_gcs.ps1`, the only backup path outside litestream's own replication, is dead as a DR mechanism: it's hardcoded to one developer's local Windows path (`c:\Users\nadhi\Cluade connect\AML\*`), is manual-only (no schedule/cron wires it up), and zips the workspace rather than the live database — it would not have helped with the Aug 19 outage and isn't a real second line of defense today.
- The legacy fallback snapshot `gs://<bucket>/amlkit.db` that `entrypoint.sh` falls back to when litestream restore fails is a frozen, pre-litestream artifact — nothing writes to that path anymore (confirmed via grep), so it was already 3 days stale during the Aug 19 outage and gets staler by the day; the next litestream failure recovers to an even older state with no way to notice until it happens.
- `litestream.yml` sets no explicit `retention`/`snapshot-interval`, and nothing periodically verifies the replica actually restores cleanly — the Aug 19 corrupted segment ("decode page 7575: EOF") was only discovered because it took prod down, not proactively. A scheduled `litestream restore` to a scratch path (cron or CI) that fails loudly on corruption would catch this before it's an outage.
- `entrypoint.sh` has caused two production incidents (litestream config syntax, then the Aug 19 crash-loop) but has zero automated coverage — `.github/workflows/source-canary.yml` only runs the Python test suite and upstream-source checks, nothing lints or exercises the shell recovery logic (restore-fails → fallback → legacy snapshot) that keeps being the actual point of failure.

### 2026-08-21

- Health check: **UNKNOWN (not DOWN)** — identical to yesterday, the sandbox egress proxy returned `403 Forbidden` on the CONNECT tunnel to `amlkit-720622408077.me-central1.run.app:443` before any request reached the app, for both `/` and `/about` (confirmed via `curl -v` and the proxy's own `recentRelayFailures` log, which shows repeated `connect_rejected` entries for this host). This is the second consecutive day this check has been unable to obtain real data — worth fixing at the environment/policy level (allowlist this host, or switch to Cloud Run's own uptime/metrics API) rather than reporting "unknown" indefinitely.
- Still open from yesterday, unchanged as of today: `backup_to_gcs.ps1` (hardcoded personal path, manual-only), the frozen legacy `gs://<bucket>/amlkit.db` fallback snapshot, `litestream.yml`'s missing retention/snapshot-interval + unverified restore, and `entrypoint.sh`'s untested recovery logic — none of these have been touched by the deploy-pipeline commits merged since (`#14`–`#17`, all scoped to Cloud Build/Artifact Registry auth).
- `amlkit/reporting/goaml.py` silently fills missing STR/SAR fields with plausible-looking placeholders (`"Grovisor Consultants"`, `"MLRO"`, `"N/A"` for account numbers, `"Unknown Entity"`) instead of failing loudly — a regulator-facing filing with a blank source account exports as `account_number = "N/A"` rather than blocking submission, and the module has zero test coverage (no `goaml`/`serialize_goaml` hits anywhere under `tests/`).
- `amlkit/reporting/goaml.py:84` builds `transactionnumber` from `int(datetime.now().timestamp())` (second-granularity, local time) while every other timestamp in the same function correctly uses UTC — two STRs exported within the same second collide on transaction number, and the local-vs-UTC inconsistency is its own latent bug.
- `amlkit/screening/kyt.py`'s structuring check sums *all* cash transactions in the 7-day window, including ones that already individually cleared `LARGE_CASH_THRESHOLD_AED` and fired their own `large_cash` alert — so a single large legitimate deposit plus one small unrelated transaction can trigger a `structuring` alert whose evidence (`total_aed`, `transaction_count`) misleadingly implies threshold-avoidance behavior that didn't happen.
- Deploy tooling has drifted onto two different registries: `scripts/deploy_gcp.ps1` and `scripts/cloud_shell_deploy.sh` still push `gcr.io/$ProjectId/amlkit:latest` (mutable tag, legacy Container Registry — which Google is sunsetting for new pushes), while the new `source-canary.yml` CI deploy pushes `${GCP_REGION}-docker.pkg.dev/.../amlkit:${{ github.sha }}` (Artifact Registry, immutable per-commit tag) — a manual deploy via either script no longer shares image history or commit traceability with what CI actually deploys.
- `amlkit/web/templates/str_builder.html` line ~55 calls `customer.full_name.split()[0]` unguarded for natural-person customers — a customer record with an empty/whitespace `full_name` (blockable via the onboarding form's `required` attribute, but not enforced at the DB layer for records created another way) throws `IndexError` and 500s the STR builder instead of showing a form error.

### 2026-08-22

- Health check: **UNKNOWN (not DOWN)** — third consecutive day the sandbox egress proxy returns `403 Forbidden` on the CONNECT tunnel to `amlkit-720622408077.me-central1.run.app:443` before any request reaches the app (confirmed via `curl -v` and the proxy's own `recentRelayFailures` log: `connect_rejected`, "gateway answered 403 to CONNECT"). Still no real uptime data obtained from this environment; this needs fixing at the policy/allowlist level or by switching to Cloud Run's own uptime metrics, not repeated daily as "unknown."
- Yesterday's deploy-pipeline fix (`56fdae2`, merged as part of #17) switched CI to Docker Buildx + Artifact Registry specifically because `gcloud builds submit` has a confirmed Google client bug that breaks WIF-credential source uploads — but `scripts/deploy_gcp.ps1` and `scripts/cloud_shell_deploy.sh` (the manual/local deploy paths) still call exactly that broken `gcloud builds submit --tag gcr.io/...` command. A manual deploy today will hit the same forbidden-bucket failure the last three PRs were spent fixing for CI.
- `amlkit/risk/model.py`'s `ruleset()` loads `ruleset.yaml` into a module-level `_cache` dict once per process with no invalidation or reload path anywhere in the codebase — on a long-lived instance (local compliance-officer machine, or a warm Cloud Run container), editing the ruleset to change risk scoring takes effect only after a process restart, with no log line or indicator that the running process is still scoring against a stale ruleset version.
- Still open, unchanged since 2026-08-20/21: `goaml.py`'s silent placeholder fields (`"Grovisor Consultants"`, `"MLRO"`, `"N/A"`) and local-time transaction-number collision risk, `kyt.py`'s structuring check double-counting already-alerted large-cash transactions, `str_builder.html`'s unguarded `full_name.split()[0]`, `backup_to_gcs.ps1`'s dead hardcoded-path DR mechanism, and `litestream.yml`'s missing retention/unverified-restore gap — none touched by this week's deploy-pipeline-only commits.
